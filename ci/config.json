pipeline {
    agent none
    stages {
        stage('Build Gateway') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build Category') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build Config') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build Data') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build Movie') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build Service') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
        stage('Build User') {
            agent { dockerfile true }
        }
            steps {
                sh 'entrypoint.sh'
        }
    }
}
