pipeline {
    agent none
    stages {
        stage('Gateway') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('Category') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('Config') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('Data') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('Movie') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('Service') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
        stage('User') {
            agent { dockerfile true }
            }
            steps {
                sh 'entrypoint.sh'
            }
        }
    }
}
